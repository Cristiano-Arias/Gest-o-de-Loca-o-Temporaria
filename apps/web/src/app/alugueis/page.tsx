import { AlugueisClient } from '@/components/alugueis-client';

// Página privada — locação de longo prazo (contratos + mensalidades).
export const dynamic = 'force-dynamic';

export default function AlugueisPage() {
  return <AlugueisClient />;
}
